apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: steady-stage
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/hitchai-app/steady-manifests
    targetRevision: stage
    path: .
  destination:
    server: https://kubernetes.default.svc
    namespace: steady-stage
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
