# Valkey instance for Sentry
# Caching and Celery task queue for Sentry workers
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sentry-valkey
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-10"
spec:
  project: default
  source:
    repoURL: https://github.com/hitchai-app/gitops
    targetRevision: master
    path: infrastructure/sentry/valkey
  destination:
    server: https://kubernetes.default.svc
    namespace: sentry-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
