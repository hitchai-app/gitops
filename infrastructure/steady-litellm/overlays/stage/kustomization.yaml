apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: steady-stage

namePrefix: steady-stage-

resources:
- ../../base

patches:
- patch: |-
    - op: replace
      path: /spec/cluster/name
      value: steady-stage-postgres
  target:
    kind: Database
    name: litellm-db
