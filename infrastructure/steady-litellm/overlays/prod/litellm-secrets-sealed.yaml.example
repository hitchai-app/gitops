# Example SealedSecret for LiteLLM master key
#
# To create the actual sealed secret:
#
# kubectl create secret generic litellm-secrets \
#   --from-literal=master-key=$(openssl rand -base64 32) \
#   --dry-run=client -o yaml | \
#   kubeseal --format=yaml > litellm-secrets-sealed.yaml
#
# Then add to kustomization.yaml resources

apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: litellm-secrets
  namespace: steady-prod
spec:
  encryptedData:
    master-key: AgA...  # Your encrypted master key here
