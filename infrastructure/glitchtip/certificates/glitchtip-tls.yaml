apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: glitchtip-tls
  namespace: glitchtip-system
spec:
  secretName: glitchtip-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - glitchtip.ops.last-try.org
