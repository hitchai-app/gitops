apiVersion: v1
kind: Service
metadata:
  name: glitchtip-web
  namespace: glitchtip-system
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: glitchtip
    component: web
