apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: arc-systems

# Helm chart is now managed by ArgoCD Application (multi-source)
# No resources needed here - only values.yaml is referenced
