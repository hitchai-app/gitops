apiVersion: v1
kind: Service
metadata:
  name: oauth2-proxy-signoz
  namespace: observability-signoz
spec:
  type: ClusterIP
  selector:
    app: oauth2-proxy-signoz
  ports:
    - name: http
      port: 4180
      targetPort: http
      protocol: TCP
