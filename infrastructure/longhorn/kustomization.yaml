apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - storageclass-single-replica.yaml
  - storageclass-replicated.yaml
  - storageclass-ephemeral.yaml
  - recurring-backup-weekly.yaml
  # Note: backup-secret-sealed.yaml deprecated - using Crossplane-managed B2 credentials
  # See infrastructure/crossplane-providers/b2-longhorn-backup.yaml
  - oauth2-proxy/deployment.yaml
  - oauth2-proxy/service.yaml
  - oauth2-proxy/cookie-secret-sealed.yaml
  - oauth2-proxy/ingress.yaml
