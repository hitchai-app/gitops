apiVersion: v1
kind: Service
metadata:
  name: youtrack
  namespace: youtrack-system
spec:
  # Headless: this is the StatefulSet's governing service (per spec.serviceName)
  clusterIP: None
  selector:
    app: youtrack
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
