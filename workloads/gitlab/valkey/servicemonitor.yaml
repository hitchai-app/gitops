apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: gitlab-valkey
  namespace: gitlab-system
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: gitlab-valkey
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics
